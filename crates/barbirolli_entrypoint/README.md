# barbirolli_entrypoint

The small static guest init process installed into prepared Firecracker
rootfs images. It launches the configured guest process and keeps the guest
serial lifecycle predictable.
