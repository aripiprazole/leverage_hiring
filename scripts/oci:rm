#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/lib/common.sh"
source "$SCRIPT_DIR/lib/oci.sh"

usage() {
    cat <<'EOF'
Usage: x oci:rm <name:tag>

Remove a stopped OCI image lifecycle and its retained artifacts.

Example:
  x oci:rm alpine:latest
EOF
}

if (($# == 0)); then
    usage_error "missing OCI image"
fi

case "$1" in
    help | --help | -h)
        usage
        exit 0
        ;;
esac

(($# == 1)) || usage_error "unexpected extra argument: $2"
parse_oci_reference "$1"
api_request POST "/oci/rm" "$(oci_reference_payload)"
